```bash
cd ~/storage/shared/Documents/obsidian-main-vault && git stash && git pull origin HEAD:master && git stash pop && git add * && git commit -m "manuale telefono" && git push origin HEAD:master
```

